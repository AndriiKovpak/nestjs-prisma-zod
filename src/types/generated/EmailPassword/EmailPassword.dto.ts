import { ApiProperty } from '@nestjs/swagger';

export class EmailPasswordDto {
  email: string;
  passwordHash: string;
  @ApiProperty({
    type: 'string',
    format: 'date-time',
  })
  createdAt: Date;
  @ApiProperty({
    type: 'string',
    format: 'date-time',
  })
  updatedAt: Date;
}
